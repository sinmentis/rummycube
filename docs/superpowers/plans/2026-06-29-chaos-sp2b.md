# SP2b 砸牌 UI + 修 SP2a minor
SUB-SKILL: subagent-driven. 后端 SP2a 已: junk2/3/4 -> pendingJunk + respondJunk stage(acceptJunk/transferJunk, shield清链, 超时默认). UI 暂无.
## Global: gate mode==='chaos'; 复用 useAbilityPlay/AbilityCard; 英文; trailer; 0 lint err; RTL+CSS-source.
## T1: junk 可出 + nextTarget 越界修
useAbilityPlay 加 junk2/3/4(同 peek 选目标)-> playAbilityCard(card.id,pid). AbilityHand junk 设 playable. moves.js: transferJunk 校验 nextTarget<numPlayers; 二次砸覆盖 pendingJunk 防呆. 测+commit.
## T2: 来袭/转移面板(respondJunk)
JunkAlert.jsx: pendingJunk.target===me 时显 "incoming +N from X" + Accept(acceptJunk) / Transfer(选自己junk+目标 transferJunk) + 软时钟; 旁观显 X owes N. shield 自动. RTL. Board 接 G.pendingJunk. CSS. gate+commit.
## T3: 全量 jest/build/lint/boot smoke. 完结.
