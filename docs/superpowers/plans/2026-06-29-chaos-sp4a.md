# 混乱 DLC · SP4a(小丑炸弹 · 后端引信)实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development(推荐)或 superpowers:executing-plans 逐任务实现。步骤用 `- [ ]`。

**Goal:** 给 chaos 模式加"小丑炸弹"后端引信:谁在自己回合**改动了含 joker 的桌上组**,在 `onTurnEnd` 结算时按 push-your-luck 概率掷一次引信;中了 → 该组连 joker **整组散回牌库** + **触发者摸 3 张普通牌**。纯后端、服务器权威、确定性随机;经典模式字节级零回归。可见"危险度"UI(joker 牌背景色 cool→hot + 小丑表情 + hover 显精确赔率)留给 **SP4b**。

**已锁规则(06 §4/§5):**
- **埋雷**:把 joker 打上桌 = 种下一个含 joker 的活组;**种下那一下不掷**(arm at heat 0)。
- **引信**:谁在自己回合**改动了**某含 joker 的组(增/删/拆/挪/取回——本计划锁为"**组成员变化**",见下)→ 结算掷一次:`p = min(0.80, 0.20 + 0.15·(heat−1))`(heat=本 joker 累计被改动次数,1-based)。即 **首次 20% / +15% 每次 / 封顶 80%**(heat 1→20%,2→35%,3→50%,4→65%,5+→80%)。
- **爆炸**:中了 → 该组(含 joker)整组散回 `tilesPool` + **当前玩家摸 3 张普通牌**。
- **重置**:joker 被取回手 → 该 joker 的 heat 删除(再打 = 新雷、heat 0)。两张 joker = 两个独立雷。只放着不动 = 不掷(无倒计时)。
- **铁律(07 §1.7)**:效果在 turn-commit(onTurnEnd)结算、不做可撤销中间态;所有惩罚摸牌只摸普通牌(`tilesPool` 本就只有普通牌,能力卡在独立的 `G.abilityDeck`,天然满足);随机走 boardgame.io `random`。

**"改动"的精确定义(本计划锁的旋钮,自动整理免疫):** 一个 joker 的"组" = 它在 `extractSeqs` 里所在的那条**同行连续 run**;"改动" = 该 run 里**非 joker 成员 id 的集合发生变化**(增删/拆分/joker 换组)。**只比成员、不比位置** → 自动整理引擎(只重排位置、不改成员)**不会误触**。纯"挪整组但成员不变"视作装饰、不计(旋钮,06 已授权)。

**Architecture:** 全部状态进 `G.jokerHeat = { [jokerTileId]: {heat, members} }`(`members` = 该 joker 当前组的非 joker 成员 id 升序数组,作为"上次结算时的基线")。新增纯 helper `src/rummikub/abilities/jokerBomb.js` `settleJokerBombs({G, ctx, random, events})`,在 `turn.js` 的 `onTurnEnd` 里、`checkGameOver` **之前**调用(chaos only)。基线随每次结算更新,故**无需另存回合起始快照**。`playerView` 不改(jokerHeat 是公开桌面状态,经 cloneDeep 自然透出给 SP4b)。

**Tech Stack:** boardgame.io 0.50 / Jest 29。纯 JS,无新依赖。

**Spec:** `06-chaos-dlc.md`(§4 小丑炸弹 / §5 数值)、`07-architecture-and-ux.md`(§1.5)。

## Global Constraints
- **经典零回归**:`settleJokerBombs` 与 setup 的 jokerHeat 初始化都 gate 在 `G.mode==='chaos'`;`mode!=='chaos'` 时 onTurnEnd 行为与现在完全一致。既有测试为护栏。
- **确定性**:引信掷骰用 boardgame.io `random.Number()`,**绝不用 Math.random**。`onTurnEnd` 需加 `random` 参数(boardgame.io 给 turn 钩子传 random —— SP1a 已在 `onTurnBegin` 用过并上线验证)。
- **完整性**:结算在 onTurnEnd(turn-commit),不可撤销;boom 摸牌从 `G.tilesPool` 出(只普通牌)。
- **概率公式(锁)**:`p(heat) = Math.min(0.80, 0.20 + 0.15*(heat-1))`,heat≥1;heat 是该 joker 累计被改动次数。
- 代码/标识符/注释/commit 英文;Conventional Commits + trailer `Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>`。lint 不新增 error(基线 0 error)。

## 已核对的真实接缝
- `extractSeqs(G)`(`moveValidation.js`)→ `string[][]`,每条是某行的连续 run(按行分组、列连续切段);tile id 是字符串。
- `isJoker(tileId:number)`(`util.js` ← `tile/codec.js`)。
- `BOARD_GRID_ID='b'`、`HAND_GRID_ID='h'`、`HAND_ROWS=2`、`HAND_COLS=22`(`constants.js`);board tile 即 `tilePositions[id].gridId==='b'`;pool=`G.tilesPool`(number[])。
- `pushTilesToGrid(tiles, HAND_ROWS, HAND_COLS, G, {gridId:HAND_GRID_ID, playerID}, ctx)`(`orderTiles.js`)= drawTile 用的入手助手,boom 摸 3 复用它。
- 散组:`delete G.tilePositions[idStr]` + `G.tilesPool.push(Number(idStr))`。
- `onTurnEnd({G, ctx, events})`(`turn.js`)当前仅 `timerExpireAt=null` + `checkGameOver(G,ctx,events)`;wire 点在 checkGameOver **之前**。setup 的 chaos 字段在 `Game.js` 的 `abilityFields`(line 41-48)。

## File Structure
- **Create** `src/rummikub/abilities/jokerBomb.js` — `settleJokerBombs`(+ 导出纯子函数 `jokerGroups`、`fuseProb` 供单测)。
- **Modify** `src/rummikub/Game.js` — setup 的 chaos `abilityFields` 加 `jokerHeat: {}`。
- **Modify** `src/rummikub/turn.js` — `onTurnEnd` 加 `random`,在 checkGameOver 前调 `settleJokerBombs`。
- **Modify** `src/tests/server-graph-dom-free.test.js` — SERVER_GRAPH_MODULES 加 `jokerBomb.js`。
- **Tests** `src/tests/joker-bomb.test.js`(helper 单测)、`joker-bomb-integration.test.js`(makeMatch 真引擎)。

---

### Task 1: `settleJokerBombs` 引信 helper(`jokerBomb.js`)

**Files:** Create `src/rummikub/abilities/jokerBomb.js`;Test `src/tests/joker-bomb.test.js`。
**Interfaces — Produces:**
- `fuseProb(heat) -> number` = `Math.min(0.80, 0.20 + 0.15*(heat-1))`(heat≥1)。
- `jokerGroups(tilePositions) -> { [jokerIdStr]: {seqIds:string[], members:number[]} }`:对每个在板 joker,返回它所在 run 的全部 tile id 串(`seqIds`,含 joker,供散组)与非 joker 成员升序 number 数组(`members`,作基线比较)。
- `settleJokerBombs({G, ctx, random, events}) -> void`:就地改 G。`mode!=='chaos'` 直接 return。逻辑:
  1. `groups = jokerGroups(G.tilePositions)`。
  2. **清理**:`G.jokerHeat` 里不在 `groups` 的 joker(被取回手/已离场)→ 删除(heat 重置)。
  3. 对每个在板 joker `jid`:
     - 若 `G.jokerHeat[jid]` 不存在 → **刚种下**:`G.jokerHeat[jid] = {heat:0, members: groups[jid].members}`,**不掷**。
     - 否则若 `!sameMembers(entry.members, groups[jid].members)` → **本回合被改动**:`entry.heat += 1`;`if (random.Number() < fuseProb(entry.heat))` → **BOOM**:`groups[jid].seqIds` 全部 `delete G.tilePositions[id]` 并 `G.tilesPool.push(Number(id))`;`delete G.jokerHeat[jid]`;`boomCount++`。否则(没炸):`entry.members = groups[jid].members`(更新基线)。
     - 否则(成员没变)→ 不掷、基线不变。
  4. boom 后:当前玩家**每个 boom 摸 3 张**——`drawNormalTiles(G, ctx, 3*boomCount)`:从 `G.tilesPool.pop()` 取(最多到空)放进 `pushTilesToGrid(...HAND_GRID_ID, ctx.currentPlayer...)`。
- `sameMembers(a,b)` = 长度相等且逐元素相等(都已升序)。

- [ ] **Step 1: 写失败测试(纯函数,确定性,无 immer)**
```js
// src/tests/joker-bomb.test.js
import {settleJokerBombs, fuseProb, jokerGroups} from '../rummikub/abilities/jokerBomb';
import {RedJoker} from '../rummikub/util';

const J = RedJoker; // joker tile id (=14, verified); 5/7/8/90/91/101.. are non-joker
// helper: place tiles on board row 0 starting col c
function boardRow(ids, row = 0, startCol = 0) {
  const tp = {};
  ids.forEach((id, i) => { tp[id] = {id, gridId: 'b', row, col: startCol + i}; });
  return tp;
}
const ctx = {currentPlayer: '0'};
const noBoom = {Number: () => 0.99};   // never < p
const alwaysBoom = {Number: () => 0.0}; // always < p
const events = {};

test('fuseProb: 20/35/50/65/80 cap', () => {
  expect(fuseProb(1)).toBeCloseTo(0.20);
  expect(fuseProb(2)).toBeCloseTo(0.35);
  expect(fuseProb(3)).toBeCloseTo(0.50);
  expect(fuseProb(4)).toBeCloseTo(0.65);
  expect(fuseProb(5)).toBeCloseTo(0.80);
  expect(fuseProb(9)).toBeCloseTo(0.80); // cap
});

test('jokerGroups: finds the joker run + its non-joker members', () => {
  const tp = boardRow([5, J, 7]); // a run 5-J-7
  const g = jokerGroups(tp);
  expect(Object.keys(g)).toEqual([String(J)]);
  expect(g[String(J)].members).toEqual([5, 7]);
  expect(g[String(J)].seqIds.map(Number).sort((a,b)=>a-b)).toEqual([5, 7, J].sort((a,b)=>a-b));
});

test('plant: a brand-new board joker arms at heat 0 and never rolls', () => {
  const G = {mode: 'chaos', tilePositions: boardRow([5, J, 7]), tilesPool: [], jokerHeat: {}};
  settleJokerBombs({G, ctx, random: alwaysBoom, events});
  expect(G.jokerHeat[String(J)]).toEqual({heat: 0, members: [5, 7]});
  expect(G.tilePositions[String(J)]).toBeDefined(); // not boomed
});

test('unchanged group: no roll even with alwaysBoom', () => {
  const G = {mode: 'chaos', tilePositions: boardRow([5, J, 7]), tilesPool: [], jokerHeat: {[String(J)]: {heat: 3, members: [5, 7]}}};
  settleJokerBombs({G, ctx, random: alwaysBoom, events});
  expect(G.jokerHeat[String(J)].heat).toBe(3); // untouched
  expect(G.tilePositions[String(J)]).toBeDefined();
});

test('modified + survive: heat++ and baseline updates (noBoom)', () => {
  // baseline members [5,7]; now run is 5-J-7-8 (added 8)
  const G = {mode: 'chaos', tilePositions: boardRow([5, J, 7, 8]), tilesPool: [], jokerHeat: {[String(J)]: {heat: 1, members: [5, 7]}}};
  settleJokerBombs({G, ctx, random: noBoom, events});
  expect(G.jokerHeat[String(J)]).toEqual({heat: 2, members: [5, 7, 8]});
});

test('modified + boom: group scatters to pool + toucher draws 3', () => {
  const hand = {}; // current player's hand starts empty
  const G = {mode: 'chaos', tilePositions: boardRow([5, J, 7, 8]), tilesPool: [101, 102, 103, 104],
             jokerHeat: {[String(J)]: {heat: 4, members: [5, 7]}}}; // heat will become 5 -> 80%
  settleJokerBombs({G, ctx, random: alwaysBoom, events});
  // group [5,J,7,8] removed from board, pushed to pool; heat entry deleted
  for (const id of [5, J, 7, 8]) expect(G.tilePositions[String(id)]).toBeUndefined();
  expect(G.jokerHeat[String(J)]).toBeUndefined();
  // toucher drew 3 normal tiles from pool into hand grid
  const handTiles = Object.values(G.tilePositions).filter(p => p.gridId === 'h' && p.playerID === '0');
  expect(handTiles).toHaveLength(3);
  // pool: had 4, drew 3, gained 4 scattered (5,J,7,8) => 4-3+4 = 5
  expect(G.tilesPool).toHaveLength(5);
});

test('retrieve to hand: joker gone from board -> heat reset', () => {
  // jokerHeat had an entry for J, but J is no longer on the board
  const G = {mode: 'chaos', tilePositions: boardRow([5, 6, 7]), tilesPool: [], jokerHeat: {[String(J)]: {heat: 3, members: [5, 7]}}};
  settleJokerBombs({G, ctx, random: alwaysBoom, events});
  expect(G.jokerHeat[String(J)]).toBeUndefined();
});

test('classic: no-op', () => {
  const G = {mode: 'classic', tilePositions: boardRow([5, J, 7]), tilesPool: [], jokerHeat: {}};
  settleJokerBombs({G, ctx, random: alwaysBoom, events});
  expect(G.jokerHeat).toEqual({}); // untouched, no arming
  expect(G.tilePositions[String(J)]).toBeDefined();
});
```
> 说明:已实测 `RedJoker===14`、`isJoker(5/7/8/90/91/101..104)===false`、`isJoker(14)===true`、tile id 范围 1..125,故上面裸数字 id(5/7/8 普通牌、101..104 池牌)与 `RedJoker` 作 joker **直接可用**,成员/joker 区分只靠 `isJoker`。

- [ ] **Step 2: 跑红** — `npx jest src/tests/joker-bomb.test.js` FAIL(模块不存在)。
- [ ] **Step 3: 实现** `jokerBomb.js`(纯逻辑,import `extractSeqs`、`isJoker`、`pushTilesToGrid`、`HAND_ROWS/COLS/HAND_GRID_ID`;**不 import boardgame.io**)。`jokerGroups` 用 `extractSeqs({tilePositions})` 得 runs,对每条含 joker 的 run,为其中每个 joker 记 `{seqIds: run, members: run.filter(非joker).map(Number).sort}`(同一 run 多 joker → 各自 entry、members 含另一个 joker?**否**:members 只算非 joker;两 joker 同 run 时两 entry 的 seqIds 相同——见已知限制)。
- [ ] **Step 4: 跑绿 + 全量** — `npx jest src/tests/joker-bomb.test.js && npx jest 2>&1 | tail -3`。
- [ ] **Step 5: commit** — `feat(chaos): joker-bomb fuse settlement helper (heat model + boom)`。

---

### Task 2: setup 初始化 `jokerHeat`(`Game.js`)

**Files:** Modify `src/rummikub/Game.js`;Test 复用/加进 `src/tests/chaos-setup.test.js`(或新 `joker-setup.test.js`)。
**行为:** chaos 局 `G.jokerHeat = {}`;经典局**不含** `jokerHeat`。

- [ ] **Step 1: 写失败测试**
```js
// 加进 src/tests/chaos-setup.test.js
test('chaos setup initializes empty jokerHeat; classic has none', () => {
  const chaos = Rummikub.setup({ctx: {numPlayers: 2}, random: mkRandom()}, {timePerTurn: 10, chaos: true});
  expect(chaos.jokerHeat).toEqual({});
  const classic = Rummikub.setup({ctx: {numPlayers: 2}, random: mkRandom()}, {timePerTurn: 10});
  expect(classic.jokerHeat).toBeUndefined();
});
```
- [ ] **Step 2: 跑红** → **Step 3: 实现** `Game.js`:chaos 分支 `abilityFields = {mode, abilityDeck: deck, abilityHands, abilityDiscard: [], jokerHeat: {}}`(只加一字段,经典分支不动)。
- [ ] **Step 4: 跑绿 + 全量** — PASS。
- [ ] **Step 5: commit** — `feat(chaos): init empty jokerHeat in chaos setup`。

---

### Task 3: onTurnEnd 接线 + 真引擎集成测 + 全量验证

**Files:** Modify `src/rummikub/turn.js`、`src/tests/server-graph-dom-free.test.js`;Test `src/tests/joker-bomb-integration.test.js`。
**行为:** `onTurnEnd` 加 `random`,在 `checkGameOver` **之前**调 `settleJokerBombs({G, ctx, random, events})`(chaos 时;helper 内部已 gate,直接调即可)。**顺序很重要**:boom 会给当前玩家摸 3 张 → 必须在 checkGameOver 前,这样"动了 joker 的制胜手被炸 → 不算赢"(已锁的"碰它就是赌命",见已知后果)。

- [ ] **Step 1: 写集成测试(makeMatch 播种 joker 板 → 真引擎跑一回合 → 验证 heat 接线)**
```js
// src/tests/joker-bomb-integration.test.js
import {Client} from 'boardgame.io/client';
import {Local} from 'boardgame.io/multiplayer';
import {makeMatch} from './__helpers__/makeMatch';
import {RedJoker} from '../rummikub/util';
import {BOARD_GRID_ID, HAND_GRID_ID} from '../rummikub/constants';

// Seed a chaos board with a joker run 5-J-7 already on the table, jokerHeat empty.
test('a planted joker arms (heat 0) after a turn settles through the real onTurnEnd', () => {
  const seeded = makeMatch({
    mode: 'chaos', jokerHeat: {},
    abilityHands: {'0': [], '1': []}, abilityDeck: [], abilityDiscard: [], peekGrants: {}, shields: {},
    tilePositions: {
      5: {id: 5, gridId: BOARD_GRID_ID, row: 0, col: 0},
      [RedJoker]: {id: RedJoker, gridId: BOARD_GRID_ID, row: 0, col: 1},
      7: {id: 7, gridId: BOARD_GRID_ID, row: 0, col: 2},
      // each seat keeps a hand tile so ending a turn doesn't trigger checkGameOver
      90: {id: 90, gridId: HAND_GRID_ID, playerID: '0', col: 0, row: 0},
      91: {id: 91, gridId: HAND_GRID_ID, playerID: '1', col: 0, row: 0},
    },
    firstMoveDone: [true, true],
  });
  const c0 = Client({game: seeded, numPlayers: 2, playerID: '0', multiplayer: Local()});
  const c1 = Client({game: seeded, numPlayers: 2, playerID: '1', multiplayer: Local()});
  c0.start(); c1.start();
  c0.events.endPhase();   // -> play (opens seat 1)
  c1.events.endTurn();    // end player 1's turn -> onTurnEnd settles, then player 0 current

  // The joker was on the board through a full settlement and was NOT modified -> armed at heat 0.
  const G = c0.getState().G;
  expect(G.jokerHeat[String(RedJoker)]).toEqual({heat: 0, members: [5, 7]});
});

test('a modified joker group is rolled through the real onTurnEnd (random reaches the hook)', () => {
  // Seed a baseline mismatch: jokerHeat says members were [5,7], but the board run is 5-J-7-8.
  // The first settlement (player 1's onTurnEnd) sees the change -> increments + rolls. Outcome is
  // non-deterministic (p<=80%, can't force from the Client), but it MUST consume random without
  // crashing, proving random is threaded into onTurnEnd. Either it boomed (entry gone, group in
  // pool) or it survived (heat 2, baseline updated).
  const seeded = makeMatch({
    mode: 'chaos', jokerHeat: {[String(RedJoker)]: {heat: 1, members: [5, 7]}},
    abilityHands: {'0': [], '1': []}, abilityDeck: [], abilityDiscard: [], peekGrants: {}, shields: {},
    tilePositions: {
      5: {id: 5, gridId: BOARD_GRID_ID, row: 0, col: 0},
      [RedJoker]: {id: RedJoker, gridId: BOARD_GRID_ID, row: 0, col: 1},
      7: {id: 7, gridId: BOARD_GRID_ID, row: 0, col: 2},
      8: {id: 8, gridId: BOARD_GRID_ID, row: 0, col: 3}, // the added tile (baseline was [5,7])
      90: {id: 90, gridId: HAND_GRID_ID, playerID: '0', col: 0, row: 0},
      91: {id: 91, gridId: HAND_GRID_ID, playerID: '1', col: 0, row: 0},
    },
    tilesPool: [101, 102, 103, 104],
    firstMoveDone: [true, true],
  });
  const c0 = Client({game: seeded, numPlayers: 2, playerID: '0', multiplayer: Local()});
  const c1 = Client({game: seeded, numPlayers: 2, playerID: '1', multiplayer: Local()});
  c0.start(); c1.start();
  c0.events.endPhase();
  c1.events.endTurn(); // onTurnEnd settles the modified joker -> rolls (consumes random)

  const e = c0.getState().G.jokerHeat[String(RedJoker)];
  // boomed -> undefined; survived -> heat 2 with updated baseline. No crash == random was passed.
  expect(e === undefined || (e.heat === 2 && e.members.join(',') === '5,7,8')).toBe(true);
});
```
> 真引擎里无法确定性强制 boom(random 由引擎播种、Client 不能注入);确定性的 plant/modify/boom 由 Task 1 单测覆盖。本集成测证**接线**:`onTurnEnd` 真调到 `settleJokerBombs`、`extractSeqs` 在真棋盘上工作、`jokerHeat` 被填充;第二个 case 额外驱动一次 modify→roll,证 **`random` 确实传进了 onTurnEnd**(已离线实测 onTurnEnd 收到 `random.Number`,此测再做端到端确认)。对齐 `arrange-move.test.js`:`events.endPhase` + `events.endTurn`(event 非 move)。

- [ ] **Step 2: 跑红** — FAIL(jokerHeat 未被填充,因为 onTurnEnd 还没接 helper)。
- [ ] **Step 3: 实现** — `turn.js`:`function onTurnEnd({G, ctx, events, random})`;函数体在 `checkGameOver(G, ctx, events)` 前插 `settleJokerBombs({G, ctx, random, events})`(import 自 `./abilities/jokerBomb.js`)。
- [ ] **Step 4: DOM-free 守卫** — `server-graph-dom-free.test.js` 的 SERVER_GRAPH_MODULES 加 `'../rummikub/abilities/jokerBomb.js'`;确认仍 PASS(jokerBomb 纯逻辑,无 document/window/navigator)。
- [ ] **Step 5: 全量门禁**
```
npx jest 2>&1 | tail -6            # 全绿(新增 ~3 测试文件 + 既有不回归)
npm run build 2>&1 | tail -3       # OK
npm run lint 2>&1 | tail -3        # 无新 error(基线 0)
PORT=9373 node --env-file=.env src/server.js & echo $! > /tmp/sp4.pid; sleep 2; curl -s localhost:9373/games; echo; kill "$(cat /tmp/sp4.pid)"  # ["RummyCube"]
```
- [ ] **Step 6: commit** — `feat(chaos): settle joker bombs in onTurnEnd before game-over; SP4a backbone complete`。

---

## 最终验证(合并前)
- [ ] `npx jest` 全绿;`npm run build` OK;`npm run lint` 无新 error;server `/games`==`["RummyCube"]`。
- [ ] **经典零回归**:`G.mode==='classic'` 时 onTurnEnd 行为不变、无 jokerHeat 字段;既有 turn / game-over 测试全绿即证。
- [ ] whole-branch review → finishing-a-development-branch(ff-merge)→ 部署(纯后端,console-error smoke 非必须但跑一下无妨)。

## 已知限制 / 后续 / 待 owner 确认
- **可见 UI 在 SP4b**:joker 牌背景色 cool→hot + 小丑表情 happy→angry(随 heat/p 渐进)+ hover 显精确赔率(读公开的 `G.jokerHeat`,playerView 已自然透出)。本计划玩家暂时**看不到危险度**,但引信已生效。
- **"碰它就是赌命"后果(已锁,但提醒)**:结算在 checkGameOver 之前 → 若制胜手改动了 joker 组且引信中了,会被炸回 3 张、**当回合不算赢**。这是 06 锁定的设计(joker 最强但最险);若 owner 想"制胜手豁免引信"是个旋钮,提出即可。
- **"改动"= 成员集合变化**(自动整理免疫)。纯"挪整组、成员不变"不计(旋钮)。若 owner 想让"挪位置"也算,需改判定(会与自动整理纠缠,需额外排除逻辑)。
- **两张 joker 同一条 run**(如 5-J-8-J-10)= 两个 entry 但 `seqIds` 相同;若其一先 boom 散掉整条 run,另一个 entry 在**下个结算**被"清理"分支删除(同回合内不重复摸牌)。罕见边角,可接受;要更精确再调。
- **heat 粒度**=每"改动回合"+1(不是每次 tmp 拖拽),符合"结算掷一次/不可撤销中间态";跨回合反复捅同一组会持续升温。

## Self-Review
- **Spec 覆盖**:埋雷不掷→plant 分支;引信概率→`fuseProb` + 改动判定;爆炸散组+摸3→boom 分支;取回重置→清理分支;两 joker 独立→按 jokerId 各自 entry;经典零回归→helper+setup gate;铁律(turn-commit / 普通牌 / random)→onTurnEnd 时机 + tilesPool + `random.Number()`。
- **数值一致**:`fuseProb(heat)=min(.8,.2+.15(heat-1))`,heat 1..5+ → 20/35/50/65/80,与 06 §5 锁值一致。
- **接缝真实**:extractSeqs/isJoker/pushTilesToGrid/onTurnEnd/tilesPool 均已核对;helper 不 import boardgame.io(免 native-ESM 目录导入坑);进 DOM-free 守卫名单。
- **测试真实**:helper 纯函数确定性单测(plant/unchanged/modify-survive/modify-boom/retrieve/classic/概率表),无 immer original 依赖;集成测证 onTurnEnd 接线 + random 透传(plant 路径不依赖 random,故确定)。
- **无占位符**:每步含可运行测试 + 实现 + 命令。
