# 混乱 DLC · SP4b(小丑危险度 UI + 两-joker 去重)实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development。步骤用 `- [ ]`。

**Goal:** 把 SP4a 的隐形引信变成**可见危险度**:桌上每个 joker 牌**背景色 cool→hot**(象牙→琥珀→红)+ **小丑表情 happy→annoyed→furious** 随热度渐进;**hover 弹出精确赔率**(下次碰它的 boom%)。读公开的 `G.jokerHeat`(playerView 自然透出)。顺手修 SP4a 已知小缺陷:两 joker 同组重复爆的去重。经典模式零回归(无 jokerHeat = 普通 joker)。

**Visual source of truth:** `mockups/sp1.html` panel 6(~1377-1420 + CSS 604-622):`.jtile.heat-low{#f3ead0}` / `.heat-med{#eaa53c}` / `.heat-high{#d23a2b}`+glow;表情 😄/😠/😡;hover `.jheat-pop`(DANGER + %  + 进度条 + 一行 note)。

**Tech Stack:** React 18 / Jest 29 + RTL。无新依赖。
**Spec:** 06 §4、07 §3(小丑危险度:背景色+表情共同表达,精确数值只 hover)。

## Global Constraints
- **经典零回归**:危险度只在 `G.jokerHeat[tileId]` 存在时渲染;经典无 jokerHeat → joker 牌外观不变。既有 board/tile 测试为护栏。
- **危险度映射(锁)**:展示"再碰一次"的 boom 概率 = `fuseProb(heat+1)`(heat=已改动次数):heat0→20%,1→35%,2→50%,3→65%,4+→80%。level:low(≤20)/med(35–50)/high(≥65);表情 low😄 / med😠 / high😡。
- 文案英文;Conventional Commits + trailer;无新依赖;lint 不新增 error。
- **已核对接缝**:`fuseProb` 由 `abilities/jokerBomb.js` 导出;Board→GridContainer(board,gridId='b')→GridSlot→Tile;Tile 已 `isJoker(tile)` 特判渲染 faSmileBeam;`G.jokerHeat` 是公开桌面态。

## File Structure
- **Create** `src/rummikub/abilities/jokerDanger.js` — `jokerDanger(heat)`(纯,复用 fuseProb)。
- **Modify** `Tile.jsx`、`GridContainer.jsx`、`GridSlot.jsx`、`Board.jsx` — 把 heat 透到棋盘 joker;heat 牌渲危险色/表情/hover。
- **Modify** `components/board.css`(或 abilities.css) — heat 配色/表情/popover。
- **Modify** `abilities/jokerBomb.js` — 两 joker 同组去重(boomedRuns)。
- **Tests** `joker-danger.test.js`、`joker-tile-heat.test.js`(+CSS-source)、加 joker-bomb.test.js 一条去重。

---

### Task 1: `jokerDanger` helper + 两-joker boom 去重

**Files:** Create `src/rummikub/abilities/jokerDanger.js`;Modify `src/rummikub/abilities/jokerBomb.js`;Test `src/tests/joker-danger.test.js` + 加进 `joker-bomb.test.js`。
**Produces:** `jokerDanger(heat) -> {prob, level, face, note}`:`prob=fuseProb(heat+1)`;`level= prob<=0.20?'low':prob<=0.50?'med':'high'`;`face= low'😄'/med'😠'/high'😡'`;note 三句(抄 mockup:low"Freshly seeded · safe to build on." / med"Poked a few times · getting risky." / high"Hot — likely to scatter if disturbed.")。
**dedupe:** `settleJokerBombs` 加 `boomedRuns` Set,一条 run 已炸则跳过其余该 run 的 joker(防双爆双摸)。

- [ ] **Step 1: 写失败测试**
```js
// src/tests/joker-danger.test.js
import {jokerDanger} from '../rummikub/abilities/jokerDanger';
test('heat -> prob/level/face', () => {
  expect(jokerDanger(0)).toMatchObject({prob: 0.20, level: 'low', face: '😄'});
  expect(jokerDanger(1)).toMatchObject({prob: 0.35, level: 'med'});
  expect(jokerDanger(3)).toMatchObject({prob: 0.65, level: 'high', face: '😡'});
  expect(jokerDanger(9).prob).toBeCloseTo(0.80); // cap
});
```
```js
// add to joker-bomb.test.js: two jokers same run -> single boom, draws 3 not 6
test('two jokers in one run boom once (dedupe)', () => {
  const G = {mode:'chaos', tilePositions: (()=>{const tp={};[5,14,7,30,9].forEach((id,i)=>tp[id]={id,gridId:'b',row:0,col:i});return tp;})(),
    tilesPool:[101,102,103,104,105,106], jokerHeat:{'14':{heat:4,members:[5,7,9]},'30':{heat:4,members:[5,7,9]}}};
  settleJokerBombs({G, ctx:{currentPlayer:'0'}, random:{Number:()=>0}, events:{}});
  const hand = Object.values(G.tilePositions).filter(p=>p.gridId==='h'&&p.playerID==='0');
  expect(hand).toHaveLength(3); // not 6
});
```
- [ ] **Step 2-4:** 红 → 实现 jokerDanger + boomedRuns 去重 → 绿 + 全量。
- [ ] **Step 5: commit** — `feat(chaos): joker danger mapping + dedupe two-joker boom`。

---

### Task 2: 棋盘 joker 牌危险度渲染(色/脸/hover)

**Files:** Modify `Tile.jsx`、`GridContainer.jsx`、`GridSlot.jsx`、`Board.jsx`、`board.css`;Test `joker-tile-heat.test.js`(+CSS-source)。
**行为:** Board 把 `jokerHeat={G.jokerHeat}` 传给**棋盘** GridContainer;GridContainer 对每格 `heat = jokerHeat?.[tile]?.heat`(joker 才有)透到 GridSlot→Tile;Tile 在 `isJoker(tile) && heat!=null` 时加 `.jtile.heat-{level}` + 表情 + hover `.jheat-pop`(DANGER + prob% + bar + note),用 `jokerDanger(heat)`。经典/手牌区不传 → 原样。

- [ ] **Step 1: 写失败测试(RTL)**
```js
// src/tests/joker-tile-heat.test.js
import {render} from '@testing-library/react';
import Tile from '../rummikub/components/Tile';
import {RedJoker} from '../rummikub/util';
test('joker tile with heat shows danger class + odds; none without', () => {
  const {container, rerender} = render(<Tile tile={RedJoker} jokerHeat={3}/>);
  expect(container.querySelector('.jtile.heat-high')).toBeTruthy();
  expect(container.textContent).toMatch(/80%/);
  rerender(<Tile tile={RedJoker}/>); // classic: no heat
  expect(container.querySelector('.jtile')).toBeFalsy();
});
```
CSS-source:断言 board.css 有 `.heat-low/.heat-med/.heat-high` + `.jheat-pop` hover。
- [ ] **Step 2-4:** 红 → 实现透传 + Tile heat 渲染 + CSS(抄 mockup tokens)→ 绿 + 全量(既有 board/tile 测试不回归)。
- [ ] **Step 5: 全量门禁** jest/build/lint/boot。**Step 6: commit** — `feat(chaos): joker danger meter (heat color + clown face + hover odds)`。

## 最终验证 / 后续
- jest 全绿、build OK、lint 0 error、经典 joker 牌无变化(无 jokerHeat)。whole-branch review → ff-merge → 部署(前端,console-error smoke 跑)。SP4 完结;下一 SP2(砸牌)。

## Self-Review
- 危险度=`fuseProb(heat+1)` 映射,与 06/SP4a 数值一致;经典零回归(无 jokerHeat 不渲);去重修 SP4a minor;读公开 jokerHeat,playerView 已透;hover 才显精确数值(07 §3)。
