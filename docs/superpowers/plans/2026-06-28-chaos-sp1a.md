# 混乱 DLC · SP1a(后端骨架)实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development(推荐)或 superpowers:executing-plans 逐任务实现。步骤用 `- [ ]`。

**Goal:** 给现有 Rummikub 加一个 opt-in 的 chaos 模式管线 + 能力卡引擎骨架(牌模型/牌库/发牌/隐藏/出卡),并实现头两张可玩卡 **Peek / Shield**;经典模式字节级零回归。纯后端,Client/headless 全可测;UI(卡手区/图鉴/动画)留给 SP1b 照 `mockups/sp1.html` 做。

**Architecture:** 新增 `src/rummikub/abilities/`(`cards.js` 牌模型+28 张牌库;`moves.js` `playAbilityCard`)。`Game.js` setup 读 `setupData.chaos→G.mode`,chaos 时初始化 `abilityDeck/abilityHands/abilityDiscard` 并发 2 张。`turn.js` `onTurnBegin` 在 chaos 下用**服务器 `random`** 30% 发 1 张。`playerView` 隐藏对手能力卡(含张数)、只给 presence 标;并按 `peekGrants` 放行被窥探者的手牌。所有随机走 boardgame.io `random`(确定性、防作弊);效果在出卡 move 内即时应用、不做成可撤销中间态。

**Tech Stack:** boardgame.io 0.50 / React 18 / Jest 29。纯 JS,无新依赖。

**Spec:** `06-chaos-dlc.md`(规则+数值)、`07-architecture-and-ux.md`(§1 架构、§2 子项目)。

## Global Constraints
- **经典零回归**:一切 chaos 逻辑 gate 在 `G.mode==='chaos'`;`mode!=='chaos'` 时 setup/onTurnBegin/playerView 行为与现在**完全一致**(不加任何 ability 字段到经典 G,或加空且不被读)。
- **确定性**:所有随机(发牌、drip)用 boardgame.io 的 `random`(`random.Shuffle`/`random.Number`),**绝不用 `Math.random`**。
- **完整性**:能力卡效果在 `playAbilityCard` 内即时应用;`playerID!==ctx.currentPlayer` → `INVALID_MOVE`;SP1a 只允许 `peek`/`shield`,其它类型 move 一律 `INVALID_MOVE`(其效果各自后续 SP 接)。
- **隐私**:`playerView` 对非本人**既不给能力卡内容、也不给张数**,只给 `abilityPresence[pid]=bool`。
- **数值(v1,来自 06)**:整副 28 张 = peek3/shield5/junk2×5(白)+ junk3×3/force2/wheel2/bigwind2(蓝)+ junk4×2/skip2/lock2(金);开局发 2;每回合 30% 发 1。
- 代码/标识符/注释/commit 英文;Conventional Commits + trailer `Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>`。lint 不新增 error。

## File Structure
- **Create** `src/rummikub/abilities/cards.js` — `CARD_RARITY`、`DECK_COUNTS`、`buildAbilityDeck()`。
- **Create** `src/rummikub/abilities/moves.js` — `playAbilityCard`。
- **Modify** `src/rummikub/Game.js` — setup 加 mode + ability 初始化 + 发 2;moves 注册 `playAbilityCard`。
- **Modify** `src/rummikub/turn.js` — 加纯 helper `applyChaosTurnStart`(chaos 30% drip + peek 过期),`onTurnBegin` 调它。
- **Modify** `src/rummikub/playerView.js` — 隐藏对手能力卡 + presence + peek 放行。
- **Modify** `src/rummikub/lobbyClient.js` — `createGame(count, time, chaos)`。
- **Tests** `src/tests/chaos-cards.test.js`、`chaos-setup.test.js`、`chaos-playerview.test.js`、`chaos-drip.test.js`、`chaos-playability.test.js`、`chaos-integration.test.js`。

---

### Task 1: 牌模型 + 28 张牌库(`cards.js`)

**Files:** Create `src/rummikub/abilities/cards.js`;Test `src/tests/chaos-cards.test.js`。
**Interfaces — Produces:** `CARD_RARITY: {type→'white'|'blue'|'gold'}`;`DECK_COUNTS: {type→n}`;`buildAbilityDeck() -> Card[]`,`Card = {id, type, rarity}`,id 形如 `"peek-0"`,**长度 28**。

- [ ] **Step 1: 写失败测试**
```js
// src/tests/chaos-cards.test.js
import {buildAbilityDeck, CARD_RARITY, DECK_COUNTS} from '../rummikub/abilities/cards';

test('deck has 28 cards with the v1 rarity mix', () => {
  const deck = buildAbilityDeck();
  expect(deck).toHaveLength(28);
  const byType = {};
  for (const c of deck) {
    expect(c).toHaveProperty('id');
    expect(c.rarity).toBe(CARD_RARITY[c.type]);
    byType[c.type] = (byType[c.type] || 0) + 1;
  }
  expect(byType).toEqual(DECK_COUNTS);
  const rarityCount = deck.reduce((a, c) => (a[c.rarity]++, a), {white: 0, blue: 0, gold: 0});
  expect(rarityCount).toEqual({white: 13, blue: 9, gold: 6});
  expect(new Set(deck.map(c => c.id)).size).toBe(28); // ids unique
});
```

- [ ] **Step 2: 跑红** — `npx jest src/tests/chaos-cards.test.js` → 模块不存在,FAIL。

- [ ] **Step 3: 实现**
```js
// src/rummikub/abilities/cards.js
export const CARD_RARITY = {
    peek: 'white', shield: 'white', junk2: 'white',
    junk3: 'blue', force: 'blue', wheel: 'blue', bigwind: 'blue',
    junk4: 'gold', skip: 'gold', lock: 'gold',
};
export const DECK_COUNTS = {
    peek: 3, shield: 5, junk2: 5,
    junk3: 3, force: 2, wheel: 2, bigwind: 2,
    junk4: 2, skip: 2, lock: 2,
};
// A fresh 28-card ability deck (unshuffled; Game.setup shuffles via server random).
export function buildAbilityDeck() {
    const deck = [];
    for (const type of Object.keys(DECK_COUNTS)) {
        for (let i = 0; i < DECK_COUNTS[type]; i++) {
            deck.push({id: `${type}-${i}`, type, rarity: CARD_RARITY[type]});
        }
    }
    return deck;
}
```

- [ ] **Step 4: 跑绿** — PASS。
- [ ] **Step 5: commit** — `feat(chaos): ability-card model + 28-card v1 deck`(+trailer)。

---

### Task 2: setup 接入 mode + 能力卡初始化(`Game.js`)

**Files:** Modify `src/rummikub/Game.js`;Test `src/tests/chaos-setup.test.js`。
**Interfaces — Consumes:** `buildAbilityDeck`(T1)。**Produces:** chaos 局的 `G.mode='chaos'`、`G.abilityDeck`(已洗、`28-2*numPlayers` 张)、`G.abilityHands[pid]=Card[2]`、`G.abilityDiscard=[]`。经典局 `G.mode='classic'`,**不含** ability 字段。

- [ ] **Step 1: 写失败测试**
```js
// src/tests/chaos-setup.test.js
import {Rummikub} from '../rummikub/Game';
const mkRandom = () => ({Shuffle: a => a, Number: () => 0.99}); // deterministic stub

test('chaos setup deals 2 ability cards each and tracks the deck', () => {
  const G = Rummikub.setup({ctx: {numPlayers: 3}, random: mkRandom()}, {timePerTurn: 10, chaos: true});
  expect(G.mode).toBe('chaos');
  expect(Object.keys(G.abilityHands)).toEqual(['0', '1', '2']);
  for (const pid of ['0', '1', '2']) expect(G.abilityHands[pid]).toHaveLength(2);
  expect(G.abilityDeck).toHaveLength(28 - 3 * 2);
  expect(G.abilityDiscard).toEqual([]);
});

test('classic setup is unchanged: no ability fields', () => {
  const G = Rummikub.setup({ctx: {numPlayers: 2}, random: mkRandom()}, {timePerTurn: 10});
  expect(G.mode).toBe('classic');
  expect(G.abilityDeck).toBeUndefined();
  expect(G.abilityHands).toBeUndefined();
});
```

- [ ] **Step 2: 跑红** — FAIL(`G.mode` undefined)。

- [ ] **Step 3: 实现** — `Game.js` 顶部加 `import {buildAbilityDeck} from "./abilities/cards.js";`。在 setup 的 `return {...}` 之前算好,并把字段并进返回对象:
```js
        // Chaos DLC: opt-in mode plumbing + ability-card backbone.
        const mode = (setupData && setupData.chaos) ? 'chaos' : 'classic';
        let abilityFields = {mode};
        if (mode === 'chaos') {
            const deck = random.Shuffle(buildAbilityDeck());
            const abilityHands = {};
            for (let p = 0; p < ctx.numPlayers; p++) {
                abilityHands[p.toString()] = [deck.pop(), deck.pop()];
            }
            abilityFields = {mode, abilityDeck: deck, abilityHands, abilityDiscard: []};
        }
        return {
            ...abilityFields,
            // 既有字段(timePerTurn / timerExpireAt / tilesPool / ... / stats)全部原样保留,
            // 一行不改、不要重复写;只在最前面多了上面这行 ...abilityFields。
            timePerTurn: (setupData ? setupData.timePerTurn : 10) * 1000,
            // ...(其余既有字段不动)
```
(把 `...abilityFields,` 放在 return 对象开头,其余既有字段一字不改。)

- [ ] **Step 4: 跑绿 + 既有 setup 不回归** — `npx jest src/tests/chaos-setup.test.js && npx jest 2>&1 | tail -3`。
- [ ] **Step 5: commit** — `feat(chaos): setup mode toggle + deal 2 ability cards (chaos only)`。

---

### Task 3: playerView 隐藏对手能力卡 + presence + peek 放行

**Files:** Modify `src/rummikub/playerView.js`;Test `src/tests/chaos-playerview.test.js`。
**Interfaces — Produces:** view 中 `abilityHands` 只保留本人;新增 `abilityPresence[pid]=bool`(对手有无卡,**无张数**);`abilityDeck` 抹成空;按 `G.peekGrants[viewer]` 放行被窥探者的手牌 tile。

- [ ] **Step 1: 写失败测试**
```js
// src/tests/chaos-playerview.test.js
import {playerView} from '../rummikub/playerView';
import {HAND_GRID_ID} from '../rummikub/constants';

const baseG = () => ({
  mode: 'chaos',
  tilePositions: {
    5: {id: 5, gridId: HAND_GRID_ID, playerID: '0', col: 0, row: 0},
    6: {id: 6, gridId: HAND_GRID_ID, playerID: '1', col: 0, row: 0},
  },
  abilityHands: {'0': [{id: 'peek-0', type: 'peek', rarity: 'white'}], '1': [{id: 'shield-0', type: 'shield', rarity: 'white'}, {id: 'skip-0', type: 'skip', rarity: 'gold'}]},
  abilityDeck: [{id: 'x'}], abilityDiscard: [],
});

test('opponent ability hands + counts are hidden; presence only', () => {
  const v = playerView({G: baseG(), ctx: {currentPlayer: '0'}, playerID: '0'});
  expect(v.abilityHands['0']).toHaveLength(1);     // own kept
  expect(v.abilityHands['1']).toBeUndefined();     // opponent content hidden
  expect(v.abilityPresence).toEqual({'1': true});  // presence, NO count, self omitted
  expect(v.abilityDeck).toEqual([]);               // deck hidden
});

test('peek grant lets the viewer see the target hand tiles', () => {
  const G = baseG(); G.peekGrants = {'0': '1'};   // player 0 peeked player 1
  const v = playerView({G, ctx: {currentPlayer: '0'}, playerID: '0'});
  expect(v.tilePositions['6']).toBeDefined();      // player 1's hand tile now visible
});
```

- [ ] **Step 2: 跑红** — FAIL(`abilityPresence` undefined / tile 6 stripped)。

- [ ] **Step 3: 实现** — 改 `playerView.js`:
  1. `stripHandTilePositions` 增加一个"放行集合"参数:
```js
function stripHandTilePositions(tilePositions, viewerID, seeIDs = null) {
    const result = {};
    for (const [tileId, pos] of Object.entries(tilePositions)) {
        if (pos.gridId === HAND_GRID_ID) {
            const owner = pos.playerID == null ? null : pos.playerID.toString();
            const allow = viewerID != null && owner === viewerID
                || (seeIDs && owner != null && seeIDs.has(owner));
            if (allow) result[tileId] = pos;
        } else {
            result[tileId] = pos;
        }
    }
    return result;
}
```
  2. `playerView` 末尾(return 前)加能力卡处理:
```js
    // Chaos: peek grants let the viewer see specific opponents' hand tiles.
    const seeIDs = new Set();
    if (G.peekGrants && viewerID != null && G.peekGrants[viewerID] != null) {
        seeIDs.add(G.peekGrants[viewerID].toString());
    }
    view.tilePositions = stripHandTilePositions(view.tilePositions, viewerID, seeIDs);
    // (上面这行替换掉原先那次 stripHandTilePositions(view.tilePositions, viewerID))

    if (G.abilityHands) {
        const presence = {};
        for (const pid of Object.keys(G.abilityHands)) {
            if (pid !== viewerID) presence[pid] = G.abilityHands[pid].length > 0;
        }
        view.abilityHands = viewerID != null && G.abilityHands[viewerID] ? {[viewerID]: G.abilityHands[viewerID]} : {};
        view.abilityPresence = presence;
        view.abilityDeck = [];
    }
```
  注意:把原本第 51 行那次 `view.tilePositions = stripHandTilePositions(view.tilePositions, viewerID)` 改成带 `seeIDs` 的版本(prevTilePositions 那次不放行 peek,保持原样)。

- [ ] **Step 4: 跑绿 + 全量** — `npx jest src/tests/chaos-playerview.test.js && npx jest 2>&1 | tail -3`(经典 playerView 测试不回归)。
- [ ] **Step 5: commit** — `feat(chaos): playerView hides opponent ability cards (+count), peek reveal`。

---

### Task 4: onTurnBegin 30% 发卡(chaos)

**Files:** Modify `src/rummikub/turn.js`;Test `src/tests/chaos-drip.test.js`。
**Interfaces — Produces:** 纯导出 helper `applyChaosTurnStart({G, seat, random})`(就地改 G:清 `peekGrants[seat]` + chaos 且牌库非空时 `random.Number()<0.3` 发 1 张给 `seat`;非 chaos 直接 return)。`onTurnBegin` 签名加 `random`,在已有逻辑末尾、`return G` 前调它。**为什么抽 helper:** 既有测试都是经 Client 间接跑 `onTurnBegin`(G 是 immer draft);直接 `onTurnBegin({...plainObject})` 会在 `original(G.tilePositions)` 处抛(immer 9 对非 draft 抛错)。helper 不碰 `original`,可在普通对象上确定性单测。

- [ ] **Step 1: 写失败测试(纯 helper,确定性,无 immer)**
```js
// src/tests/chaos-drip.test.js
import {applyChaosTurnStart} from '../rummikub/turn';

const baseG = () => ({
  mode: 'chaos', abilityDeck: [{id: 'a'}, {id: 'b'}], abilityHands: {'0': [], '1': []},
  peekGrants: {'0': '1'},
});

test('drip deals 1 card when the roll passes (chaos)', () => {
  const G = baseG();
  applyChaosTurnStart({G, seat: '0', random: {Number: () => 0.1}}); // <0.3 → deal
  expect(G.abilityHands['0']).toHaveLength(1);
  expect(G.abilityDeck).toHaveLength(1);
  expect(G.peekGrants['0']).toBeUndefined(); // own peek grant expired on own turn
});

test('drip deals nothing when the roll fails, but peek still expires', () => {
  const G = baseG();
  applyChaosTurnStart({G, seat: '0', random: {Number: () => 0.9}}); // >=0.3 → none
  expect(G.abilityHands['0']).toHaveLength(0);
  expect(G.peekGrants['0']).toBeUndefined();
});

test('non-chaos is a no-op and never throws', () => {
  const G = {mode: 'classic', abilityHands: {'0': []}, abilityDeck: [{id: 'a'}], peekGrants: {}};
  expect(() => applyChaosTurnStart({G, seat: '0', random: {Number: () => 0.1}})).not.toThrow();
  expect(G.abilityHands['0']).toHaveLength(0);
  expect(G.abilityDeck).toHaveLength(1);
});
```

- [ ] **Step 2: 跑红** — FAIL(`applyChaosTurnStart` 未导出)。

- [ ] **Step 3: 实现** — `turn.js`:加纯 helper,加进 export 块,并在 `onTurnBegin` 里调用。
```js
// Chaos DLC turn-start side effects. Pure mutation of G; safe on a plain object
// OR an immer draft (no `original`), so it unit-tests directly. Expires this
// seat's peek grant (a round elapsed) + 30% one-card drip from the ability deck.
function applyChaosTurnStart({G, seat, random}) {
    if (G.mode !== 'chaos') return;
    if (G.peekGrants) delete G.peekGrants[seat];
    if (Array.isArray(G.abilityDeck) && G.abilityDeck.length
        && random && random.Number() < 0.3) {
        if (!G.abilityHands[seat]) G.abilityHands[seat] = [];
        G.abilityHands[seat].push(G.abilityDeck.pop());
    }
}
```
  - `onTurnBegin` 签名:`function onTurnBegin({G, ctx, events, random})`。
  - 在它**末尾、`return G` 之前**插入一行:`applyChaosTurnStart({G, seat, random})`(`seat = ctx.currentPlayer` 函数开头已有)。
  - turn.js 是 barrel:helper 用**裸 `function`**(如上,不加 `export`),只在文件底部的 `export {...}` 块里加一行 `applyChaosTurnStart,`(避免重复导出报错)。

- [ ] **Step 4: 跑绿 + 全量** — PASS;`npx jest 2>&1 | tail -3` 不回归。
- [ ] **Step 5: commit** — `feat(chaos): 30% ability-card drip + peek-grant expiry in onTurnBegin`。

---

### Task 5: `playAbilityCard` move(Peek + Shield)

**Files:** Create `src/rummikub/abilities/moves.js`;Test `src/tests/chaos-playability.test.js`。
**Interfaces — Produces:** `playAbilityCard({G, ctx, playerID}, cardId, target) -> void|INVALID_MOVE`。校验:本人回合、卡在手、类型∈{peek,shield}。**peek** → `G.peekGrants[playerID]=target` 并把该卡移入 `abilityDiscard`;**shield** → `G.shields[playerID]=true` 并入 discard。其它类型/找不到卡/非本人 → `INVALID_MOVE`。SP1a 出卡**面朝上**(诈唬是 SP5)。

- [ ] **Step 1: 写失败测试(纯函数,确定性)**
> 说明:发牌是服务器随机,Client harness 里拿不到"确定的某张卡",所以 move 逻辑用**纯函数确定性单测**覆盖(强断言);端到端"move 注册后真能跑通"放 Task 6 用 `makeMatch` 播种已知卡来测。
```js
// src/tests/chaos-playability.test.js
import {playAbilityCard} from '../rummikub/abilities/moves';
const INVALID = 'INVALID_MOVE';
function gWith(cards) {
  return {mode: 'chaos', abilityHands: {'0': cards.slice(), '1': []}, abilityDiscard: [], peekGrants: {}, shields: {}};
}
test('peek resolves: grant set, card discarded', () => {
  const G = gWith([{id: 'peek-0', type: 'peek', rarity: 'white'}]);
  const r = playAbilityCard({G, ctx: {currentPlayer: '0'}, playerID: '0'}, 'peek-0', '1');
  expect(r).toBeUndefined();
  expect(G.peekGrants['0']).toBe('1');
  expect(G.abilityHands['0']).toHaveLength(0);
  expect(G.abilityDiscard.map(c => c.id)).toContain('peek-0');
});
test('shield resolves: held flag set, card discarded', () => {
  const G = gWith([{id: 'shield-0', type: 'shield', rarity: 'white'}]);
  playAbilityCard({G, ctx: {currentPlayer: '0'}, playerID: '0'}, 'shield-0');
  expect(G.shields['0']).toBe(true);
  expect(G.abilityHands['0']).toHaveLength(0);
});
test('guards: not your turn / missing card / unimplemented type / peek without target -> INVALID', () => {
  const G = gWith([{id: 'skip-0', type: 'skip', rarity: 'gold'}, {id: 'peek-0', type: 'peek', rarity: 'white'}]);
  expect(playAbilityCard({G, ctx: {currentPlayer: '1'}, playerID: '0'}, 'skip-0')).toBe(INVALID); // not your turn
  expect(playAbilityCard({G, ctx: {currentPlayer: '0'}, playerID: '0'}, 'nope')).toBe(INVALID);   // no such card
  expect(playAbilityCard({G, ctx: {currentPlayer: '0'}, playerID: '0'}, 'skip-0')).toBe(INVALID); // skip not impl in SP1a
  expect(playAbilityCard({G, ctx: {currentPlayer: '0'}, playerID: '0'}, 'peek-0')).toBe(INVALID); // peek needs a target
});
```

- [ ] **Step 2: 跑红** — FAIL(模块不存在)。

- [ ] **Step 3: 实现**
```js
// src/rummikub/abilities/moves.js
import {INVALID_MOVE} from 'boardgame.io/core';

const SP1_TYPES = new Set(['peek', 'shield']);

// Play one ability card face-up on your turn. SP1a resolves peek + shield only;
// other types reject (their effects land in later sub-projects). Bluff/face-down
// is SP5. Effect applies immediately at move time (never an undoable interim).
export function playAbilityCard({G, ctx, playerID}, cardId, target) {
    if (playerID !== ctx.currentPlayer) return INVALID_MOVE;
    const hand = G.abilityHands && G.abilityHands[playerID];
    if (!hand) return INVALID_MOVE;
    const idx = hand.findIndex(c => c.id === cardId);
    if (idx < 0) return INVALID_MOVE;
    const card = hand[idx];
    if (!SP1_TYPES.has(card.type)) return INVALID_MOVE;

    if (card.type === 'peek') {
        if (target == null) return INVALID_MOVE;
        if (!G.peekGrants) G.peekGrants = {};
        G.peekGrants[playerID] = target.toString();
    } else if (card.type === 'shield') {
        if (!G.shields) G.shields = {};
        G.shields[playerID] = true;
    }
    hand.splice(idx, 1);
    if (!G.abilityDiscard) G.abilityDiscard = [];
    G.abilityDiscard.push(card);
}
```

- [ ] **Step 4: 跑绿** — `npx jest src/tests/chaos-playability.test.js` PASS。
- [ ] **Step 5: commit** — `feat(chaos): playAbilityCard move — peek + shield (face-up)`。

---

### Task 6: 注册 move + lobby 传 chaos + 全量验证

**Files:** Modify `src/rummikub/Game.js`(moves 注册)、`src/rummikub/lobbyClient.js`;Test 复用上面 + 全量门禁。
**Interfaces — Consumes:** `playAbilityCard`(T5)。**行为:** 把 `playAbilityCard` 注册进 `Rummikub.moves`;`lobbyClient.createGame` 多收一个 `chaos` 传进 `setupData`。

- [ ] **Step 1: 实现注册** — `Game.js`:`import {playAbilityCard} from "./abilities/moves.js";`,在 `moves:{...}` 里加一行 `playAbilityCard,`。
- [ ] **Step 2: 实现 lobby 传参** — `lobbyClient.js`(注意保留既有 `unlisted: true`):
```js
    async createGame(playersCount, timePerTurn, chaos = false) {
        const result = await this.client.createMatch(GAME_NAME, {
            numPlayers: parseInt(playersCount),
            setupData: {timePerTurn: parseInt(timePerTurn), chaos: !!chaos},
            unlisted: true,
        });
        return result.matchID
    }
```
  (`createGame` 的调用方在 SP1b 的 Lobby UI 里传 `chaos`;SP1a 先把签名和透传做好,默认 `false` 不影响现有调用。)
- [ ] **Step 3: 端到端集成测试(makeMatch 播种已知卡 → 走注册的 move → 验证 reveal)**
> 这步补上纯函数测不到的"接线":move 真的注册了、经 Client 派发能改状态、playerView 真把对手手牌露出来。用 `makeMatch` 覆盖 `mode/abilityHands/tilePositions` 播种一张确定的 peek + 对手一张确定手牌。
```js
// src/tests/chaos-integration.test.js
import {Client} from 'boardgame.io/client';
import {Local} from 'boardgame.io/multiplayer';
import {makeMatch} from './__helpers__/makeMatch';
import {HAND_GRID_ID} from '../rummikub/constants';

test('a seeded peek, played through the registered move, resolves + discards (wiring)', () => {
  const seeded = makeMatch({
    mode: 'chaos',
    abilityHands: {'0': [{id: 'peek-0', type: 'peek', rarity: 'white'}], '1': []},
    abilityDiscard: [], peekGrants: {}, shields: {},
    tilePositions: {
      5: {id: 5, gridId: HAND_GRID_ID, playerID: '0', col: 0, row: 0},
      6: {id: 6, gridId: HAND_GRID_ID, playerID: '1', col: 0, row: 0},
    },
    firstMoveDone: [true, true],
  });
  const c0 = Client({game: seeded, numPlayers: 2, playerID: '0', multiplayer: Local()});
  const c1 = Client({game: seeded, numPlayers: 2, playerID: '1', multiplayer: Local()});
  c0.start(); c1.start();
  c0.events.endPhase();   // playersJoin -> play (the play phase opens on seat 1)
  c1.events.endTurn();    // end player 1's turn -> player 0 current (EVENT, unconditional, like arrange-move.test.js)

  c0.moves.playAbilityCard('peek-0', '1');                   // proves the move is registered + dispatchable
  const {G} = c0.getState();
  expect(G.peekGrants['0']).toBe('1');                       // resolved end-to-end
  expect(G.abilityHands['0']).toHaveLength(0);               // card spent
  expect(G.abilityDiscard.map(c => c.id)).toContain('peek-0'); // moved to discard
});
```
> 关键(照 `arrange-move.test.js`):用 `c0.events.endPhase()` 一次进 play(开在 seat 1),再 `c1.events.endTurn()`(**event 不是 move**,无条件结束、绕过"必须先抽牌"的 move 守卫),此后 player 0 当庄。fixture 给两家各留 1 张手牌(tile 5/6),所以结束 player 1 回合不会清空手牌触发 checkGameOver。**职责边界:** 本测只证 move 接线(注册/派发/结算/入弃牌);peek 揭示对手手牌的逻辑由 T3 的 `chaos-playerview.test.js` 在已知 master G 上确定性单测,这里不重复。

- [ ] **Step 4: DOM-free 守卫** — `abilities/cards.js`、`abilities/moves.js` 是纯逻辑;若它们进了 server-graph,确认 `src/tests/server-graph-dom-free.test.js` 不报新错(不含 `document/window/navigator` 标识符)。
- [ ] **Step 5: 全量门禁**
```
npx jest 2>&1 | tail -6            # 全绿(新增 ~6 个测试文件 + 既有不回归)
npm run build 2>&1 | tail -3       # OK
npm run lint 2>&1 | tail -3        # 无新 error(基线 0 error)
PORT=9371 node --env-file=.env src/server.js & echo $! > /tmp/sp1.pid; sleep 2; curl -s localhost:9371/games  # ["RummyCube"]; 然后 kill "$(cat /tmp/sp1.pid)"
```
- [ ] **Step 6: commit** — `feat(chaos): register playAbilityCard + lobby chaos flag; SP1a backbone complete`。

---

## 最终验证(合并前)
- [ ] `npx jest` 全绿;`npm run build` OK;`npm run lint` 无新 error;server `/games`==`["RummyCube"]`。
- [ ] 经典局零回归:`G.mode==='classic'`、无 ability 字段、onTurnBegin/playerView 行为不变(既有测试全绿即证)。
- [ ] whole-branch review → finishing-a-development-branch(ff-merge)→ 部署。
- [ ] **注意**:SP1a 无可见 UI(全靠测试驱动)。玩家可见效果在 **SP1b**(照 `mockups/sp1.html` 做卡手区 / 出卡流 / 图鉴 FAB / 模式入口 + 把 `createGame` 的 chaos 接上按钮)。

## 已知限制 / 后续
- **只 peek/shield 可玩**;其它 8 种卡在牌库/手牌/图鉴里存在但出牌被拒(各自效果:砸牌 SP2、转盘 SP3、小丑 SP4、诈唬 SP5、跳过/封族/逼牌/大风吹 SP6)。
- **shield 只设标志**;"被指定时自动抵消"的消费方在 SP2(砸牌)接入时实现。
- **peek 时长**=到你下一个回合(onTurnBegin 清),≈一圈;若要精确"回合数"再调。

## Self-Review
- **Spec 覆盖**:模式管线(§1.1)→T2/T6;能力卡状态+隐藏(§1.2)→T2/T3;30% 发卡→T4;playAbilityCard+peek/shield→T5;确定性/完整性铁律(§1.7)→Global+各任务用 `random`、INVALID_MOVE、即时应用。28 张/发2/30%(06 数值)→T1/T2/T4。
- **类型一致**:`Card={id,type,rarity}`;`abilityHands:{pid:Card[]}`;`peekGrants:{pid:targetId}`;`shields:{pid:bool}`;`abilityPresence:{pid:bool}` 全程一致。
- **零回归**:一切 gate 在 `mode==='chaos'`;经典 setup/onTurnBegin/playerView 不读 ability 字段;既有测试为护栏。`lobbyClient` 保留 `unlisted:true`。
- **无占位符**:每步含可运行测试 + 实现 + 命令。
- **测试策略(自审定稿)**:① move 逻辑用**纯函数确定性单测**(发牌是服务器随机,harness 拿不到确定卡)。② drip 抽成纯 helper `applyChaosTurnStart` 单测——直接 `onTurnBegin({plainObject})` 会在 `original(G.tilePositions)` 抛(immer 9 对非 draft 抛错,已实测),既有测试都是经 Client 间接跑它。③ 接线用一条 `makeMatch` 播种 + Client 集成测,对齐 `arrange-move.test.js`:`events.endPhase` 进 play、`events.endTurn`(event 非 move)让 player 0 当庄,两家各留 1 张手牌避免 checkGameOver。④ Game.js 无 `endIf`,空手牌不会在 turn-begin 自动判胜。
